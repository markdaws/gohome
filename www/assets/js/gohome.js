(function() {

    var App = React.createClass({
        render: function() {
            return (
                <div>Welcome to gohome</div>
            );
        }
    });

    debugger;
    React.render(<App />, document.body);
})();
